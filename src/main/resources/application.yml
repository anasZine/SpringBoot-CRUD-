spring:
 datasource:
   url: jdbc:postgresql://localhost:5432/student_db
   username: anas
   password: anas
   driver-class-name: org.postgresql.Driver
 jpa:
  hibernate:
   ddl-auto: create
  database: postgresql

  show-sql: true
  properties:
   hibernate:
    format_sql: true
  open-in-view: false


